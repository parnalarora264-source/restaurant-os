'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Order({params}:{params:Promise<{orderId:string}>}){const[o,setO]=useState<any>();useEffect(()=>{params.then(({orderId})=>fetch('/api/orders/'+orderId).then(r=>r.json()).then(d=>setO(d.order)))},[params]);return <div className="min-h-[70vh] px-6 py-20"><div className="mx-auto max-w-2xl text-center">{!o?<p>Loading your order...</p>:<><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-full bg-[#697552] text-3xl text-white">✓</div><p className="mt-6 text-xs font-bold uppercase tracking-[.3em] text-[#697552]">Order received</p><h1 className="serif mt-3 text-5xl font-semibold">We're on it.</h1><p className="mt-4 text-[#6b6258]">Order <strong>{o.id}</strong> for {o.customer}. Status: <strong>{o.status}</strong>.</p><div className="mt-10 rounded-3xl bg-[#3F4A32] p-7 text-left text-[#F5EEDF]"><div className="flex justify-between"><span>Items</span><span>{o.items.reduce((a:any,x:any)=>a+x.qty,0)}</span></div><div className="mt-3 flex justify-between text-xl font-semibold"><span>Total</span><span>₹{o.total}</span></div></div><Link href="/menu" className="mt-8 inline-block rounded-full bg-[#3F4A32] px-6 py-3 font-semibold text-white">Order something else</Link></>}</div></div>}
