'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {readCart,clearCart} from '@/lib/store';export default function Checkout(){const router=useRouter();const[name,setName]=useState('');const[table,setTable]=useState('');const[phone,setPhone]=useState('');const[loading,setLoading]=useState(false);async function submit(e:any){e.preventDefault();setLoading(true);const c=readCart();const r=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({customer:name,table,phone,items:c})});const d=await r.json();clearCart();router.push('/order/'+d.order.id)}return <div className="px-6 py-16"><div className="mx-auto max-w-2xl"><p className="text-xs font-bold uppercase tracking-[.3em] text-[#697552]">Checkout</p><h1 className="serif mt-2 text-5xl font-semibold">Let's get your order in.</h1><form onSubmit={submit} className="mt-10 grid gap-4 rounded-3xl border border-[#D9C7A8] bg-[#FBF7EF] p-7"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Name" className="rounded-2xl bg-[#F5EEDF] p-4 outline-none"/><input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Phone" className="rounded-2xl bg-[#F5EEDF] p-4 outline-none"/><input value={table} onChange={e=>setTable(e.target.value)} placeholder="Table number (optional for dine-in)" className="rounded-2xl bg-[#F5EEDF] p-4 outline-none"/><button disabled={loading} className="mt-3 rounded-full bg-[#3F4A32] py-4 font-semibold text-white disabled:opacity-60">{loading?'Placing order...':'Place order'}</button></form></div></div>}
